apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: gcps.kubecloudscaler.cloud
  annotations:
    cert-manager.io/inject-ca-from: '{{ .Release.Namespace }}/{{ include "helm.fullname"
      . }}-serving-cert'
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
  {{- include "helm.labels" . | nindent 4 }}
spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: '{{ include "helm.fullname" . }}-webhook-service'
          namespace: '{{ .Release.Namespace }}'
          path: /convert
      conversionReviewVersions:
      - v1
  group: kubecloudscaler.cloud
  names:
    kind: Gcp
    listKind: GcpList
    plural: gcps
    singular: gcp
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Gcp is the Schema for the scalers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GcpSpec defines the desired state of Scaler
            properties:
              authSecret:
                description: AuthSecret name
                type: string
              deploymentTimeAnnotation:
                description: Deployment time annotation
                type: string
              dryRun:
                description: dry-run mode
                type: boolean
              excludeResources:
                description: Exclude resources from downscaling
                items:
                  type: string
                type: array
              labelSelector:
                description: Labels selectors
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              periods:
                description: Time period to scale
                items:
                  description: ScalerPeriod defines a scaling period with time constraints
                    and replica limits.
                  properties:
                    maxReplicas:
                      description: Maximum replicas
                      format: int32
                      type: integer
                    minReplicas:
                      description: Minimum replicas
                      format: int32
                      type: integer
                    name:
                      description: Name of the period
                      pattern: ^(|[a-zA-Z0-9][a-zA-Z0-9_-]*)$
                      type: string
                    time:
                      description: TimePeriod defines the time configuration for a scaling
                        period.
                      properties:
                        fixed:
                          description: FixedPeriod defines a fixed time period for scaling
                            operations.
                          properties:
                            endTime:
                              pattern: ^\d{4}-(0?[1-9]|1[0,1,2])-(0?[1-9]|[12][0-9]|3[01])
                                ([0-1]?[0-9]|2[0-3]):[0-5]?[0-9]:[0-5]?[0-9]$
                              type: string
                            gracePeriod:
                              description: Grace period in seconds for deployments before
                                scaling down
                              pattern: ^\d*s$
                              type: string
                            once:
                              description: Run once at StartTime
                              type: boolean
                            reverse:
                              description: Reverse the period
                              type: boolean
                            startTime:
                              pattern: ^\d{4}-(0?[1-9]|1[0,1,2])-(0?[1-9]|[12][0-9]|3[01])
                                ([0-1]?[0-9]|2[0-3]):[0-5]?[0-9]:[0-5]?[0-9]$
                              type: string
                            timezone:
                              type: string
                          required:
                          - endTime
                          - startTime
                          type: object
                        recurring:
                          description: RecurringPeriod defines a recurring time period
                            for scaling operations.
                          properties:
                            days:
                              items:
                                type: string
                              type: array
                            endTime:
                              pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                              type: string
                            gracePeriod:
                              pattern: ^\d*s$
                              type: string
                            once:
                              description: Run once at StartTime
                              type: boolean
                            reverse:
                              description: Reverse the period
                              type: boolean
                            startTime:
                              pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                              type: string
                            timezone:
                              type: string
                          required:
                          - days
                          - endTime
                          - startTime
                          type: object
                      type: object
                    type:
                      enum:
                      - down
                      - up
                      type: string
                  required:
                  - time
                  - type
                  type: object
                type: array
              projectId:
                description: |-
                  Resources
                  ProjectID
                type: string
              region:
                description: Region
                type: string
              resources:
                description: Resources
                items:
                  type: string
                type: array
              restoreOnDelete:
                description: Restore on delete
                type: boolean
              waitForOperation:
                description: Wait for operation to complete
                type: boolean
            required:
            - periods
            type: object
          status:
            description: ScalerStatus defines the observed state of Scaler.
            properties:
              comments:
                type: string
              currentPeriod:
                description: ScalerStatusPeriod defines the current period status for
                  a scaler.
                properties:
                  failed:
                    items:
                      description: ScalerStatusFailed represents a failed scaling operation.
                      properties:
                        kind:
                          type: string
                        name:
                          type: string
                        reason:
                          type: string
                      required:
                      - kind
                      - name
                      - reason
                      type: object
                    type: array
                  spec:
                    description: RecurringPeriod defines a recurring time period for
                      scaling operations.
                    properties:
                      days:
                        items:
                          type: string
                        type: array
                      endTime:
                        pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                        type: string
                      gracePeriod:
                        pattern: ^\d*s$
                        type: string
                      once:
                        description: Run once at StartTime
                        type: boolean
                      reverse:
                        description: Reverse the period
                        type: boolean
                      startTime:
                        pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                        type: string
                      timezone:
                        type: string
                    required:
                    - days
                    - endTime
                    - startTime
                    type: object
                  specSHA:
                    type: string
                  success:
                    items:
                      description: ScalerStatusSuccess represents a successful scaling
                        operation.
                      properties:
                        comment:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    type: array
                  type:
                    type: string
                required:
                - spec
                - specSHA
                - type
                type: object
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Gcp is the Schema for the scalers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GcpSpec defines the desired state of Scaler
            properties:
              authSecret:
                description: AuthSecret name
                type: string
              defaultPeriodType:
                default: down
                description: Default status for resources
                enum:
                - down
                - up
                type: string
              dryRun:
                description: dry-run mode
                type: boolean
              periods:
                description: Time period to scale
                items:
                  description: ScalerPeriod defines a scaling period with time constraints
                    and replica limits.
                  properties:
                    maxReplicas:
                      description: Maximum replicas
                      format: int32
                      type: integer
                    minReplicas:
                      description: Minimum replicas
                      format: int32
                      type: integer
                    name:
                      description: Name of the period
                      pattern: ^(|[a-zA-Z0-9][a-zA-Z0-9_-]*)$
                      type: string
                    time:
                      description: TimePeriod defines the time configuration for a scaling
                        period.
                      properties:
                        fixed:
                          description: FixedPeriod defines a fixed time period for scaling
                            operations.
                          properties:
                            endTime:
                              pattern: ^\d{4}-(0?[1-9]|1[0,1,2])-(0?[1-9]|[12][0-9]|3[01])
                                ([0-1]?[0-9]|2[0-3]):[0-5]?[0-9]:[0-5]?[0-9]$
                              type: string
                            gracePeriod:
                              description: Grace period in seconds for deployments before
                                scaling down
                              pattern: ^\d*s$
                              type: string
                            once:
                              description: Run once at StartTime
                              type: boolean
                            reverse:
                              description: Reverse the period
                              type: boolean
                            startTime:
                              pattern: ^\d{4}-(0?[1-9]|1[0,1,2])-(0?[1-9]|[12][0-9]|3[01])
                                ([0-1]?[0-9]|2[0-3]):[0-5]?[0-9]:[0-5]?[0-9]$
                              type: string
                            timezone:
                              type: string
                          required:
                          - endTime
                          - startTime
                          type: object
                        recurring:
                          description: RecurringPeriod defines a recurring time period
                            for scaling operations.
                          properties:
                            days:
                              items:
                                type: string
                              type: array
                            endTime:
                              pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                              type: string
                            gracePeriod:
                              pattern: ^\d*s$
                              type: string
                            once:
                              description: Run once at StartTime
                              type: boolean
                            reverse:
                              description: Reverse the period
                              type: boolean
                            startTime:
                              pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                              type: string
                            timezone:
                              type: string
                          required:
                          - days
                          - endTime
                          - startTime
                          type: object
                      type: object
                    type:
                      enum:
                      - down
                      - up
                      type: string
                  required:
                  - time
                  - type
                  type: object
                type: array
              projectId:
                description: ProjectID
                type: string
              region:
                description: Region
                type: string
              resources:
                description: Resources
                properties:
                  labelSelector:
                    description: Labels selectors
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector requirements.
                          The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector applies
                                to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  names:
                    description: Names of resources to manage
                    items:
                      type: string
                    type: array
                  types:
                    description: |-
                      Types of resources
                      K8s: deployments, statefulsets, ... (default: deployments)
                      GCP: VM-instances, ... (default: vm-instances)
                    items:
                      type: string
                    type: array
                type: object
              restoreOnDelete:
                default: true
                description: 'Restore resource state on CR deletion (default: true)'
                type: boolean
              waitForOperation:
                description: Wait for operation to complete
                type: boolean
            required:
            - periods
            - projectId
            - resources
            type: object
          status:
            description: ScalerStatus defines the observed state of Scaler.
            properties:
              comments:
                type: string
              currentPeriod:
                description: ScalerStatusPeriod defines the current period status for
                  a scaler.
                properties:
                  failed:
                    items:
                      description: ScalerStatusFailed represents a failed scaling operation.
                      properties:
                        kind:
                          type: string
                        name:
                          type: string
                        reason:
                          type: string
                      required:
                      - kind
                      - name
                      - reason
                      type: object
                    type: array
                  spec:
                    description: RecurringPeriod defines a recurring time period for
                      scaling operations.
                    properties:
                      days:
                        items:
                          type: string
                        type: array
                      endTime:
                        pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                        type: string
                      gracePeriod:
                        pattern: ^\d*s$
                        type: string
                      once:
                        description: Run once at StartTime
                        type: boolean
                      reverse:
                        description: Reverse the period
                        type: boolean
                      startTime:
                        pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                        type: string
                      timezone:
                        type: string
                    required:
                    - days
                    - endTime
                    - startTime
                    type: object
                  specSHA:
                    type: string
                  success:
                    items:
                      description: ScalerStatusSuccess represents a successful scaling
                        operation.
                      properties:
                        comment:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    type: array
                  type:
                    type: string
                required:
                - spec
                - specSHA
                - type
                type: object
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - name: v1alpha3
    schema:
      openAPIV3Schema:
        description: Gcp is the Schema for the gcps API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Gcp
            properties:
              config:
                description: GcpConfig defines the configuration for GCP resource management.
                properties:
                  authSecret:
                    description: AuthSecret name
                    type: string
                  defaultPeriodType:
                    default: down
                    description: Default status for resources
                    enum:
                    - down
                    - up
                    type: string
                  projectId:
                    description: ProjectID
                    type: string
                  region:
                    description: Region
                    type: string
                  restoreOnDelete:
                    default: true
                    description: 'Restore resource state on CR deletion (default: true)'
                    type: boolean
                  waitForOperation:
                    description: Wait for operation to complete
                    type: boolean
                required:
                - projectId
                type: object
              dryRun:
                description: dry-run mode
                type: boolean
              periods:
                description: Time period to scale
                items:
                  description: ScalerPeriod defines a scaling period with time constraints
                    and replica limits.
                  properties:
                    maxReplicas:
                      description: Maximum replicas
                      format: int32
                      type: integer
                    minReplicas:
                      description: Minimum replicas
                      format: int32
                      type: integer
                    name:
                      description: Name of the period
                      pattern: ^(|[a-zA-Z0-9][a-zA-Z0-9_-]*)$
                      type: string
                    time:
                      description: TimePeriod defines the time configuration for a scaling
                        period.
                      properties:
                        fixed:
                          description: FixedPeriod defines a fixed time period for scaling
                            operations.
                          properties:
                            endTime:
                              pattern: ^\d{4}-(0?[1-9]|1[0,1,2])-(0?[1-9]|[12][0-9]|3[01])
                                ([0-1]?[0-9]|2[0-3]):[0-5]?[0-9]:[0-5]?[0-9]$
                              type: string
                            gracePeriod:
                              description: Grace period in seconds for deployments before
                                scaling down
                              pattern: ^\d*s$
                              type: string
                            once:
                              description: Run once at StartTime
                              type: boolean
                            reverse:
                              description: Reverse the period
                              type: boolean
                            startTime:
                              pattern: ^\d{4}-(0?[1-9]|1[0,1,2])-(0?[1-9]|[12][0-9]|3[01])
                                ([0-1]?[0-9]|2[0-3]):[0-5]?[0-9]:[0-5]?[0-9]$
                              type: string
                            timezone:
                              type: string
                          required:
                          - endTime
                          - startTime
                          type: object
                        recurring:
                          description: RecurringPeriod defines a recurring time period
                            for scaling operations.
                          properties:
                            days:
                              items:
                                type: string
                              type: array
                            endTime:
                              pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                              type: string
                            gracePeriod:
                              pattern: ^\d*s$
                              type: string
                            once:
                              description: Run once at StartTime
                              type: boolean
                            reverse:
                              description: Reverse the period
                              type: boolean
                            startTime:
                              pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                              type: string
                            timezone:
                              type: string
                          required:
                          - days
                          - endTime
                          - startTime
                          type: object
                      type: object
                    type:
                      enum:
                      - down
                      - up
                      type: string
                  required:
                  - time
                  - type
                  type: object
                type: array
              resources:
                description: Resources
                properties:
                  labelSelector:
                    description: Labels selectors
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector requirements.
                          The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector applies
                                to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  names:
                    description: Names of resources to manage
                    items:
                      type: string
                    type: array
                  types:
                    description: |-
                      Types of resources
                      K8s: deployments, statefulsets, ... (default: deployments)
                      GCP: VM-instances, ... (default: vm-instances)
                    items:
                      type: string
                    type: array
                type: object
            required:
            - periods
            - resources
            type: object
          status:
            description: status defines the observed state of Gcp
            properties:
              comments:
                type: string
              currentPeriod:
                description: ScalerStatusPeriod defines the current period status for
                  a scaler.
                properties:
                  failed:
                    items:
                      description: ScalerStatusFailed represents a failed scaling operation.
                      properties:
                        kind:
                          type: string
                        name:
                          type: string
                        reason:
                          type: string
                      required:
                      - kind
                      - name
                      - reason
                      type: object
                    type: array
                  spec:
                    description: RecurringPeriod defines a recurring time period for
                      scaling operations.
                    properties:
                      days:
                        items:
                          type: string
                        type: array
                      endTime:
                        pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                        type: string
                      gracePeriod:
                        pattern: ^\d*s$
                        type: string
                      once:
                        description: Run once at StartTime
                        type: boolean
                      reverse:
                        description: Reverse the period
                        type: boolean
                      startTime:
                        pattern: ^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$
                        type: string
                      timezone:
                        type: string
                    required:
                    - days
                    - endTime
                    - startTime
                    type: object
                  specSHA:
                    type: string
                  success:
                    items:
                      description: ScalerStatusSuccess represents a successful scaling
                        operation.
                      properties:
                        comment:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    type: array
                  type:
                    type: string
                required:
                - spec
                - specSHA
                - type
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
